/**
 * Enum representing different available HTTP request methods.
 */
export enum HttpRequestMethod {
    // eslint-disable-next-line no-unused-vars
    GET = 'get',
    // eslint-disable-next-line no-unused-vars
    POST = 'post',
    // eslint-disable-next-line no-unused-vars
    PUT = 'put',
    // eslint-disable-next-line no-unused-vars
    DELETE = 'delete',
}
